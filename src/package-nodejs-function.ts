// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for PackageNodejsFunction
 */
export interface PackageNodejsFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/package-nodejs.
 */
export class PackageNodejsFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: PackageNodejsFunctionProps) {
    super(scope, id, {
      description: 'src/package-nodejs.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../assets/package-nodejs.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}